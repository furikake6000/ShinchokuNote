<svg
  width="<%= size %>"
  height="<%= size %>"
  viewBox="0 0 100 100"
  style="fill: <%= defined? color ? color : 'black' %>">
  <use xlink:href="<%= asset_path "Shinchoku_icon.svg" %>#icon" />
</svg>
