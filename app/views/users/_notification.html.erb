<div class="">
    <% case notify[:type] %>
    <% when 'Comment' %>
        <%= render notify[:content], with_note_info: true  %>
    <% when 'ShinchokuDodeskas' %>
        「<%= notify[:note].name %>」に新しく<%= notify[:shinchoku_dodeskas].size %>件の進捗どうですかが投稿されました！<br>
        <% notify[:shinchoku_dodeskas].each do |s| %>
            <% if s.from_user %>
                <%= link_to image_tag(s.from_user.thumb_url, alt: s.from_user.name, class: 'mr-1 note-info-user-thumb rounded-circle', size: '32x32'), user_path(s.from_user.screen_name) unless s.from_user.thumb_url.blank? %>
            <% end %>
        <% end %>
    <% when 'Watchlists' %>
        「<%= notify[:note].name %>」は新しく<%= notify[:watchlists].size %>人のウォッチリストに登録されました！
        <% notify[:watchlists].each do |w| %>
            <% if w.watching_user %>
                <%= link_to image_tag(w.watching_user.thumb_url, alt: w.watching_user.name, class: 'mr-1 note-info-user-thumb rounded-circle', size: '32x32'), user_path(w.watching_user.screen_name) unless w.watching_user.thumb_url.blank? %>
            <% end %>
        <% end %>
    <% end %>
</div>
<hr>